# Production values must be supplied by the deployment platform.
PAYLOAD_SECRET=replace-with-a-cryptographically-random-secret
DATABASE_URI=postgresql://user:password@localhost:5432/sudeposu
NEXT_PUBLIC_SITE_URL=https://example.com
